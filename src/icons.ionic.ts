import { addIcons } from "ionicons";
import { ellipse, square, triangle, chevronForwardOutline } from "ionicons/icons";

export function bootstrapIonIcons() {
  addIcons({ triangle, ellipse, square, chevronForwardOutline });
}
